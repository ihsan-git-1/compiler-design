package ast.variables;

import ast.nodes.Node;

public class Expression extends Node {


    public Expression(int line, String parent, String type, int childCount) {
        super(line, parent, type, childCount);
    }
}
